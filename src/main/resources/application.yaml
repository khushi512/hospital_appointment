server:
  port: 8081

spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-square-bonus-a1r6xrrp-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_GAgr6ujw7NkU}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    enabled: true
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:your-email@gmail.com}
    password: ${MAIL_PASSWORD:your-app-password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  sql:
    init:
      mode: never

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:hospital-appointment-secret-key-123456}
  expiration: 86400000

# File Upload Configuration
file:
  upload-dir: uploads/appointments/